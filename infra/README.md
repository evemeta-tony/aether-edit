# infra/

Infrastructure for aether-edit.

- `ci/` (delivered by WO-001R): the check scripts run by GitHub Actions and by
  `infra/ci/check-all.sh` locally, plus their exception and mapping files.

Deployment and provisioning content arrives in later WOs, starting with
WO-014 (the original WO-001 items 2 through 5).
